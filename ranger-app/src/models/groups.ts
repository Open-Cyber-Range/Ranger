
export type AdGroup = {
  id: string;
  name: string;
  description?: string;
};

export type AdUser = {
  id: string;
  username: string;
  email?: string;
  firstName?: string;
  lastName?: string;
};

